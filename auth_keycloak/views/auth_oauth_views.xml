<?xml version="1.0"?>
<odoo>
    <record id="view_oauth_provider_form" model="ir.ui.view">
        <field name="name">auth.oauth.provider.form</field>
        <field name="model">auth.oauth.provider</field>
        <field name="inherit_id" ref="auth_oauth.view_oauth_provider_form"/>
        <field name="arch" type="xml">
            <field name="client_id" position="after">
                <field name="client_secret"/>
            </field>
            <xpath expr="//field[@name='auth_endpoint']/ancestor::group[1]" position="after">
                <field invisible="1" name="id"/>
                <!-- TODO: any good way to hide/show this properly -->
                <group string="Users management (Keycloak)">
                    <field name="users_endpoint" placeholder="http://keycloak.mycompany.com/auth/admin/realms/{realm}/users"/>
                    <field name="superuser" placeholder="admin"/>
                    <field name="superuser_pwd" placeholder="I hope is not &quot;admin&quot;"/>
                    <field name="users_management_enabled"/>
                    <button attrs="{'invisible': [('users_management_enabled', '=', False)]}" context="{'default_provider_id': id}" name="%(auth_keycloak.keycloak_sync_users)d" string="Sync users" type="action"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>